apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
  namespace: keycloak
  annotations: 
    rgocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  ingress:
    enabled: false
  instances: 1
  db:
    vendor: postgres
    host: postgres
    usernameSecret:
      name: keycloak-db-secret
      key: username
    passwordSecret:
      name: keycloak-db-secret
      key: password
  http:
    httpEnabled: true
    hostname:
      hostname: https://keycloak-route-keycloak.apps-crc.testing
    proxy:
      headers: xforwarded 

    #tlsSecret: "" # Let OpenShift handle TLS termination via the Route
  unsupported:
    podTemplate:
      spec:
        containers:
          - env:
            - name: JDK_JAVA_OPTIONS
              value: "-XX:UseSVE=0"
